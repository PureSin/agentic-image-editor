# z.AI API credentials
Z_AI_API_KEY=your-api-key-here

# Model configuration — swap to try different multimodal models
# z.AI models: GLM-4.6V, GLM-5
# Any OpenAI-compatible model also works via the same base URL pattern
MODEL=GLM-4.6V
API_BASE=https://api.z.ai/api/paas/v4/
